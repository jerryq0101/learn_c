#include <stdio.h>
#include <ctype.h>

int bruh(a) {
        return a;
}

int main(void) {
        double a = 5.1;
        int b = 3;

        // printf("%f", a*b);

        // printf("%d", isdigit('1'));
        // putchar(tolower('A'));


        // printf("%d", b=a);

        char c = 'a';
        printf("%c", bruh(c));

        return 0;
}
